// Since the user declined Firebase, we will export null or dummy versions
// if they change their mind later, but for now we use our custom backend.
export const db = null;
export const auth = null;
export const app = null;
